# Frontend (React PWA)

Features:
- React app (JavaScript)
- PWA manifest + service worker registration (workbox)
- Leaflet map integration
- Basic login and dashboard components

To run:
1. cd frontend
2. npm install
3. npm start

Note: Service worker is set up for production builds (workbox). For dev, offline behavior is limited.
