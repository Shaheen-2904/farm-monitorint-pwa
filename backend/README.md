# Backend (Spring Boot)

Features:
- Spring Boot REST API
- JPA entities and repository for Farm
- Demo auth endpoint (stub) at /api/auth/login

To run:
1. Install MySQL and create database `farmdb` (or adjust application.properties)
2. mvn -f backend/pom.xml spring-boot:run
